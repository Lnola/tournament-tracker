# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install pnpm
        run: |
          curl -L https://unpkg.com/@pnpm/self-installer@latest | node
          export PATH=$HOME/.pnpm-global/bin:$PATH

      - name: Install dependencies and build
        run: |
          (pnpm i && pnpm build)

      - name: Deploy
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}

        # uses: FirebaseExtended/action-hosting-deploy@v0
        # with:
        #   repoToken: '${{ secrets.GITHUB_TOKEN }}'
        #   firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TOURNAMENT_TRACKER_E1A65 }}'
        #   projectId: tournament-tracker-e1a65
